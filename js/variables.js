
const a = 1234; // number, (simpelt number)
const b = "1234"; // string, (could be letters in this too)
const c = 123.4; // number, with komma
const d = [1, 2, 3, 4] // array, is data object
const e = { type: "Fiat", model: "500", color: "white" }; // object
const f = true; // boolean, is true
const g = null; // object (why is null an object class?)
const h = undefined; // Undefined
const i = function () { }; // Function, to do something
const j = new Date(); // object, with Date

// Const var og let -- forskel

console.log("variabel a er:" + typeof (a));
console.log("variabel b er:" + typeof (b));
console.log("variabel c er:" + typeof (c));
console.log("variabel d er:" + typeof (d));
console.log("variabel e er:" + typeof (e));
console.log("variabel f er:" + typeof (f));
console.log("variabel g er:" + typeof (g));
console.log("variabel h er:" + typeof (h));
console.log("variabel i er:" + typeof (i));
console.log("variabel j er:" + typeof (j));